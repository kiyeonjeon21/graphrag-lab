name: "Quick Test"
description: "Single framework quick test for CI/smoke testing"

dataset:
  name: sample_articles
  path: data/sample

frameworks:
  - name: fast

queries:
  - "What are the main themes discussed across all documents?"

llm:
  model: gpt-5.4-mini

evaluation:
  metrics: []
